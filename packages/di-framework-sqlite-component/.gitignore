# Rust build output
/target/

# Hermetic toolchain installed by scripts/install-tools.sh
/.tools/

# Built artifacts are produced by `make build`; publish them via release
# assets / OCI, not git.
/dist/
